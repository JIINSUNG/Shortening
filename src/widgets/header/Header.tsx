import React from "react";
import styles from "./Header.module.css";
const Header = () => {
  return (
    <header className={styles.header}>
      <h2 className={styles.h2}>광고 없는 심플 URL 단축기, 쇼트닝</h2>
    </header>
  );
};

export default Header;
